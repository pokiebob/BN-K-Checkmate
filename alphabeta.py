def alphabeta_policy(depth, h):
    def policy(pos):
        # value, move = alphabeta(pos, depth, h, alpha, beta)
        return
    return policy


def alphabeta(pos, depth, h, alpha, beta):
    pass
